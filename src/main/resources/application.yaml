spring:
  application:
    name: 'urlShortener'
  datasource:

    url: 'jdbc:h2:mem:testdb'
    username: sa
    password: password
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
logging:
  level:
    de.schroeer.b: debug